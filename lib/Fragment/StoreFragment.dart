import 'package:flutter/cupertino.dart';

class StoreFragment extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
          child: Text("This is my Store Fragment")
      ),
    );
  }
}